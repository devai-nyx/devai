# DEVAI-II (wireframe)

**This is a layout wireframe for the DEVAI-II succession — NOT the genesis.** The real
bootstrap (BR-1) begins only after R-Ω closes devai-original and the Owner authorization
binds D-189. See `work/devai-ii-succession-dossier.md` for the complete program.

Layout per dossier Part VII (final form): `law/` `product/` `work/` `docs/` `record/`
`packages/` `tests/` `scratch/` + hidden `.devai/`.
