# Figure 25 — Evidence chain and dark promotion

## Purpose

Two lanes of the rule of written record: the hash-chained ledger, and the three-beat dark-promotion arc for Actions-run evidence.

## Visual structure

- **Top group "Hash-chained evidence ledger"**: four EV-record boxes (harness palette, "prev_hash + content hash") chained left→right by "hash link" arrows; caption "append-only · verify walks the chain · redaction is itself a recorded, attributable operation"; an Auditor/verify box (observer) reads the chain via a "read-only" arrow.
- **Bottom group "Dark promotion — new power in three beats (D-146 → D-164 → D-165)"**: 1·Enact (ctrl; gate mode = verify, dark) → 2·Verify in shadow (sup; exact-tree tuples, full CI still runs) → 3·Graduate (human; eight-condition conjunct, Owner ruling) → Activate (neutral, dashed arrow, "separate role-gated batches (future)").
- Footer captions: vacatio legis reading (promoted runs never count toward the soak, D-163) and the shadow-commissioning control reading.
- **Aspect**: ~1200×560.

## Authoring notes

Authored via `docs/theory/diagrams/render-figures.py::fig25_evidence_chain`.
Re-rendering: `python3 docs/theory/diagrams/render-figures.py`.
Output: `docs/theory/diagrams/svg/fig-25-evidence-chain.svg`.
