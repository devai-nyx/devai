---
adr_id: ADR-<SCOPE>-<NNNN>
title: <Title in Title Case>
status: Proposed
date: YYYY-MM-DD
authors: ['@your-handle']
tags: [<scope-tag>, <area-tag>]
---

# ADR-<SCOPE>-<NNNN> — <Title>

**Authority:** Architect.
**Related:** `../path/to/related-contract.md`, `../path/to/predecessor-adr.md` (replace with real link targets).

## Status

Proposed.

<!--
When accepted, replace with:
Accepted YYYY-MM-DD.

When rejected, replace with:
Rejected YYYY-MM-DD. Reason: <one paragraph>.

When superseded, replace with:
Superseded by `ADR-<SCOPE>-<NNNN>` (link to the successor file) YYYY-MM-DD.
-->

## Context

<!--
What situation produced the need for this decision? Cite the surfacing artifact:
ticket id, audit finding, scorecard cell, sensor reading, sibling repo's
pattern, etc. Keep this section concrete — abstract context produces abstract
decisions.

Two to four paragraphs is usually right.
-->

## Decision

<!--
State the decision affirmatively. "We will adopt X." or "X is the canonical
form for Y." One paragraph if possible — longer means you're describing
mechanism, which belongs under Consequences or in a sibling spec.
-->

## Consequences

<!--
What falls out of the decision? Group as:

- **Invariants that emerge** — new INV-* identifiers, or constraints on existing ones.
- **Work unblocked** — features, refactors, or follow-on ADRs that become possible.
- **Downsides accepted** — what gets harder, and why we accept that.
- **Debt accepted** — what we explicitly defer.
-->

## Alternatives considered

<!--
What else was on the table? For each, one paragraph: what it was, why it lost.

- **Alternative A — <name>**: <description>. Rejected because <reason>.
- **Alternative B — <name>**: <description>. Rejected because <reason>.

If no alternatives were considered, say so explicitly — "no alternatives considered;
this is the only shape that satisfies <constraint>". That's a valid answer; an
empty section is not.
-->

## References

<!--
- Contracts: `../path/to/contract.md`
- Invariants: `INV-<DOMAIN>-<NNN>` (in `law/invariants/`)
- Predecessors / superseded by: links here
- External: issues, RFCs, papers
-->
