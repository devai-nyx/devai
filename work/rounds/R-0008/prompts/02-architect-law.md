# Wave 02 — Architect law and round contract

Role: DEVAI Architect. Effort: xhigh. Authority: only `law/**`, `work/rounds/**`, `docs/**`; obey the narrower live mandate.

Create B0 declaration, then the next free DII contract and schema/policy/profile/graph/matrix changes in plan section 3. Recheck the gapless ID immediately before commit. Derive all command closure to a fixpoint. Keep the 16 argv byte-for-byte unless the Owner separately authorizes a roster change (this round does not request one). Make protected-property non-equivalence explicit. Regenerate Architect-owned derived artifacts in the same invalidating commit. Do not create a mixed-role commit; if Engineer materialization cannot remain atomic under the approved activation mechanism, STOP.

Run schema, policy, trace, sequencing, registry, roster, prepare, full-test, tier, coverage, governance, and diff gates applicable to law. Return exact commit and all derived population/digest producer commands; never invent a digest.
