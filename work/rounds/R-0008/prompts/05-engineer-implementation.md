# Wave 05 — Engineer repair and materialization

Role: DEVAI Engineer. Effort: xhigh. Authority: exact surfaces in plan section 6 only.

Implement signed claim contracts, independent expected-identity recomputation, fail-closed cross-gate lookup, ordered claim recipes, protected-property exclusions, unambiguous provenance, and benchmark instrumentation. Do not put a private key in repository/runtime evidence. Preserve literal standalone execution and remote uncached full execution. Whole edges between ordinary/stage2/coverage remain ineligible.

Regenerate every Engineer-owned projection in the same commit that invalidates it under the B1 activation mechanism. No follow-up cleanup commit may repair a stale derived artifact. Run focused adversaries, all prior freshness/cache controls, prepare, full Vitest, all tiers, coverage at unchanged floors, sequencing, materializations, trace, governance, and diff. Any false REUSE_FRESH is STOP and rollback.
