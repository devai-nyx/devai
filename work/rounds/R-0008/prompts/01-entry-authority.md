# Wave 01 — Owner and entry authority

Role: Owner for mandate path; Architect for read-only entry verification. Effort: high. No delegation of authority.

Read the request in plan section 2. Do not convert it into a grant unless the Owner explicitly approves it. The mandate must bind R-0008, reviewer selector, two cycles, one retry, no fallback, local signer/trust policy, exact GitHub OIDC/Sigstore issuer/repository/workflow-ref/reusable-workflow/event/ref/environment/subject policy, repository visibility/plan eligibility, atomic role-pure activation, exact exclusions, and predecessor sequencing. Verify live base, required predecessor closure, clean trees, exact authority census, DII gap, baseline/coverage, and no scope-changing drift. Output a signed-off entry checklist. PENDING, ambiguous, unavailable GitHub capability, or infeasible atomic activation disables the affected reuse path or stops where required; do not create B0 without the complete bound authority.
