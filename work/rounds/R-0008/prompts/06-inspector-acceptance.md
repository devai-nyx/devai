# Wave 06 — Inspector acceptance and adversarial sweep

Role: DEVAI Inspector. Effort: xhigh.

Recompute the complete matrix population. Mutate every bound identity and signed-envelope component independently; use authorized and unauthorized fixture keys and GitHub attestation bundles; replay across candidate, policy, graph, toolchain, environment, output, order, issuer, repository, workflow/ref, reusable-workflow chain, event/ref/environment and subject changes; poison caches/artifacts/conclusions; inject invalid reuse at all sixteen ordinals and both passes. Prove fast cannot satisfy cold, authoritative cold is non-cancellable and has no DEVAI result reuse, thresholds, V8 environment, build-before-test, separate T1/T2 ordering, ordinary combined root execution, and detached literal operation. Tests must fail if any protected property is lost, even if runtime becomes faster. Return exact GREEN_PROVED mapping per row.
