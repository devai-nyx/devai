# Wave 07 — Auditor correctness, literal roster, and benchmark

Role: DEVAI Auditor. Effort: high.

At one exact clean candidate, execute the local and GitHub run method in plan section 8/B6, including every literal argv alone from empty runtime state, remote fast cold/warm dependency states, eligible/ineligible GitHub-attested claims, and the remote mandatory cold lane with DEVAI result reuse disabled. Compare complete semantic populations before duration. Record queue/setup/install/artifact/verification/job/critical-path timings and count units explicitly; the current coverage config is not T1–T3-only merely because its filename says so. Independently reproduce every reused identity and verify every local signature and GitHub bundle/subject from retained artifacts. Produce no positive saving claim unless populations are identical and the median critical path decreases. Zero/negative saving disables speed activation absent a separately evidenced security/operability benefit. Any correctness mismatch triggers rollback.
