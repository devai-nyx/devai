# Wave 07 — Auditor correctness, literal roster, and benchmark

Role: DEVAI Auditor. Effort: high.

At one exact clean candidate, execute the five-run method in plan section 8/B6, including every literal argv alone from empty runtime state. Compare complete semantic populations before duration. Record count units explicitly; the current coverage config is not T1–T3-only merely because its filename says so. Independently reproduce every reused identity and verify every signature from retained artifacts. Produce no positive saving claim unless populations are identical and the median critical path decreases. Zero/negative saving disables activation. Any correctness mismatch triggers rollback.
