# Wave 03 — Inspector complete red population

Role: DEVAI Inspector. Effort: xhigh. Authority: test paths only.

Materialize every R8-F001–F012 adversary and every R8-001–R8-026 test ID from the closure matrix. Use production entrypoints/pure verifiers, not source-string findings. Every adversary asserts a named failed check, exact gate ordinal, and exact expected/presented identity in payload. Exhaust all equivalence-key components, 16 ordinals, signature forgery/replay, GitHub issuer/repository/workflow-ref/reusable-workflow/event/ref/environment/subject mutations, cache/artifact/conclusion non-authority, fast/cold lane separation and cancellation, order dependence, property loss, ambiguous caches, false-green terminal population, atomic derivation, benchmark count naming, and clean detached standalone behavior.

Demonstrate red before repair. Do not weaken prior tests. If test changes invalidate a derived artifact, use only the already-approved same-commit role-pure mechanism or STOP. Return exact failing IDs/exits, not a summary count alone.
