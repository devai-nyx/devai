# R-0009 orchestrator prompt

Do not run until R-0007/R-0008 close and the product/docs/site authority is rebound to
R-0009. Load canonical CLI, executor, model/runtime, suite/preset, sensor/inventory,
convergence, product, history, projection, site, and deploy-refusal sources. Use role-pure
commits and risk order P0 -> P1 -> P2.

Generate enumeration tables from machine sources and write one coherent narrative corpus;
never maintain a second membership list in prose. Preserve candidate-not-release wording.
Run all docs/reference/example/link/site drift checks plus complete repository convergence.
Hash the exact built artifact, but do not deploy it. Stop on semantic bulk replacement,
generated direct edit, undocumented category, product-choice ambiguity, Pages action, or
release claim.
