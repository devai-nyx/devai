# R-0010 orchestrator prompt

Execute Phase A only after authority is rebound and R-0009 closes. Prepare exact packages,
migration, hashes, SBOM, attestations, dry runs, and handoff using role-pure batches. Keep
real stynx read-only and secrets undisclosed. Stop at A6.

Do not enter Phase B without a direct Owner grant binding the exact revalidated candidate,
transaction order, and rollback. On grant, revalidate all remote conflicts and credentials
before the first effect. Stop immediately on partial publication or subject mismatch and
preserve exact external state for recovery. Never infer release from candidate readiness.
