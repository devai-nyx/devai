# R-0008 orchestrator prompt

Do not execute without the adopted R-0008 mandate and closed R-0007. Read the exact live
policies, command closure, schemas, R-0008 plan/matrix, DII-252 amendment, execution
contract, and reviewer binding. Use a dedicated worktree and role-pure commits.

Run B0–B9 serially. Whole-gate reuse among ordinary, stage2, and coverage is forbidden.
Expected claim identity is independently reproduced before result inspection; trusted
signature plus exact identity plus fresh dependencies/outputs plus uniqueness are all
required. Anything else executes or blocks. Require executable red and immutable Auditor
evidence before repair. Stop on any false REUSE_FRESH, projection-role contradiction,
private-key exposure, threshold/property/population loss, review-budget breach, predecessor
write, or external release effect.
