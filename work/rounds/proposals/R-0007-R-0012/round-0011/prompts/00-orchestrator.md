# R-0011 orchestrator prompt

Do not run before published R-0010 closes and authority is rebound. Keep evidence
authorization reuse distinct from R-0008 convergence caching. Resolve authorization from
the exact successor first-parent object, never caller choice/current HEAD/predecessor
standing. Every missing or invalid case runs full. Metrics observe but never authorize or
relabel. Preserve zero streak and stop before any qualifying-run count or activation.
