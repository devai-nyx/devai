# Wave 6 — executable acceptance

## Agents

Spawn three fresh `gpt-5.6-sol` Inspector agents at `xhigh`:

1. Complete population and migration execution.
2. Authority/effect/DB/task adversarial verification.
3. Executor/suite/preset/sensor/inventory and output-totality verification.

## Acceptance

- Execute all 42 kept actions in every supported format and required refusal form.
- Execute all historical routes and prove no folded/tombstoned handler dispatch.
- Prove exact 42/170/11/223 registry counts from the migration fixture.
- Prove seven-domain default help and hidden plumbing discovery.
- Prove exact quick/standard/full/release suite membership and aggregate exit precedence.
- Prove exact baseline/structural/governed/sweep membership and canonical order.
- Prove sweep is read-only and reports excluded write-capable kinds.
- Exercise PostgreSQL to prove `sense migrate` requires and uses bound write authority.
- Prove every task has one round, rejects cross-round access, observes wave dependencies, and rolls back partial resources.
- Prove all four executor kinds, model-registry validation, exact/preferred/policy
  selection, unsupported-effort refusal, no implicit fallback, composite cycle
  rejection, and requested-versus-resolved execution evidence.
- Prove deterministic routines do not invoke an LLM, exact agent selection cannot
  substitute a runtime/model/effort, and legacy task records cannot execute until
  explicitly migrated.
- Prove `--publish` alone cannot publish and `--allow-publish` is rejected.
- Prove generated/source/binary parity with no skip-if-unbuilt silence.

Return defects to the owning role; do not accommodate broken implementation in tests.
