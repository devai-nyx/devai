---
id: R-0002-PLAN
title: Frozen re-bind and operational-law repair
type: round-plan
status: draft
date: 2026-07-24
authority: Architect
supersedes: null
superseded_by: null
provenance:
  [
    OM-002; R-0002-PREFLIGHT-AUDIT; R-0002-PREFLIGHT-BACKLOG; BL-001–003,
    BL-007,
    BL-012–014,
    BL-023,
    BL-046–049,
  ]
---

# R-0002 — Frozen re-bind and operational-law repair

## Objective and claims ceiling

Bind the successor to the immutable predecessor close, correct R-0001’s closure record
append-only, make production law checks truthful and non-vacuous, and restore portable
CI. R-0002 prepares founding ratification; it does not perform it.

## Entry gates

- R-0001 and predecessor R-Ω are closed.
- `AUTHORIZATION.md` reads GRANTED.
- Live predecessor commit/tree/chain-head/D-196/PC-0019 values match the preflight audit.
- Successor base equals live `origin/main`.
- The 812-test floor is green after `pnpm run devai:prepare`.
- Every local and exact-main CI gate must pass; no red exception remains authorized.

## Batches

| Batch | Role                     | Work                                                                                                                                                                                                                          | Commit gate                                                                    |
| ----- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| B0    | Architect                | Append the round declaration decision with exact base, plan digest, no-ratification ceiling, and known reds                                                                                                                   | Register parser and full floor                                                 |
| B1    | Auditor                  | Re-read immutable R-Ω sources; record final bindings, BL-002 `none-needed`, BL-003 completed acts/residuals, host state, and successor opening red                                                                            | Independent hashes/host facts agree                                            |
| B2    | Inspector                | Add failing contracts for cold-Corepack determinism, DII decision ordering, release-disposition typing/CLI path, successor ADR/glob/trace/forbidden-action behavior, attestation self-reference, and reference classification | Failures match the named defects                                               |
| B3    | Engineer                 | Close BL-046 and BL-049 without baseline laundering; pre-provision required pnpm versions; repair closure parsing/types/CLI validation; establish the OM-002 `.devai/` ignore/sentinel prerequisite for later BL-050          | New reds green; stage 2 green; stage 3 executes; runtime state stays untracked |
| B4    | Architect                | Re-bind BL-001; preserve `ratified: null`; close law portions of BL-007; record ADR vocabulary and Article-27 placement decisions; materialize BL-012/013/014 policy intent; correct Architect-owned BL-047 references        | ADR/glob/trace/register checks non-vacuous                                     |
| B5    | Engineer                 | Regenerate/materialize successor policy and trace from authoritative sources; close BL-023 runtime/type consumers; apply Engineer-owned BL-047 mapping; add generation/byte-identity checks                                   | Generated bytes match; no hand-edited materialization                          |
| B6    | Machine verb + Inspector | Emit PC-0002 superseding PC-0001 with the unmatched BL-045 deferral stated honestly; verify ledger selection                                                                                                                  | PC-0001 unchanged; PC-0002 schema and ledger green                             |
| B7    | Inspector                | Replace each exact known-red guard closed here in the same sequence; run production commands and adversarial checks                                                                                                           | No zero-match or self-reference success                                        |
| B8    | Auditor                  | Write R-0002 as-built, reconcile all twelve items, quote fresh coverage, and record every residual                                                                                                                            | No uncatalogued deferral                                                       |
| B9    | Architect + machine verb | Run the shared source-PR then closure-PR ceremony; verify exact-main is all green                                                                                                                                             | Closure says “ratifies nothing”                                                |

Role changes are separate commits. B6’s `record/` output is never hand-authored.

## Required production acceptance

In addition to the shared exit floor:

- `policy check adrs` accepts all 12 successor ADR records under the declared lifecycle;
- `policy check glob guards` executes successor paths and no required guard matches zero;
- `spec validate trace` resolves with a machine-materialized domains policy;
- forbidden-action coverage includes successor `law/` and governance paths;
- the attestation validates with final predecessor commit/tree/internal chain head,
  `repo_url` pointing to `devai-original`, closing fields set, `frozen: true`, and
  `ratified: null`;
- reference-triage output classifies every post-rename occurrence;
- cold and warm fingerprint behavior is identical;
- `.devai/config/` and `.devai/pin/` stay tracked, while runtime contents under
  `.devai/state/` and `.devai/worktrees/` are ignored except tracked `.gitkeep`
  sentinels;
- stage 3 and the unchanged 70/60/70/70 coverage floors pass.

## Stop and rollback

Stop on any immutable predecessor mismatch, attempted `../devai` write, mechanical
repository-name rewrite, fingerprint-baseline refresh without intentional behavior
change, widened noise exemption, law floor weakening, PC-0001 edit, or ratification
claim. Roll back only the last role-pure batch.

## Exit handoff to R-0003

The close record carries:

- exact rebound attestation digest;
- production-command results;
- PC-0002 correction identity;
- fresh all-green coverage readings;
- exact merged SHA and exact-main CI;
- an explicit statement that R-0003 must independently re-verify all of them before
  ratification.
