# R-0002 ORCHESTRATOR — frozen re-bind and operational-law repair

You are the R-0002 orchestrator in the DEVAI successor repository. Execute the
repository-scoped round to completion; do not ratify or release.

## Read before action

1. `product/owner-mandates/OM-002.md`
2. `product/owner-mandates/OM-003.md`
3. `product/owner-mandates/OM-004.md`
4. `product/owner-mandates/OM-005.md`
5. `work/audit/R-0002-preflight/consolidated-audit.md`
6. `work/audit/R-0002-preflight/backlog-register.md`
7. `work/rounds/EXECUTION-CONTRACT.md`
8. `work/rounds/R-0002/AUTHORIZATION.md`
9. `work/rounds/R-0002/plan.md`
10. R-0001 as-built, combined freshness audit, PC-0001, current attestation, law
    regeneration status, and every known-red file named by the scoped items

If authorization is not GRANTED or immutable R-Ω values do not re-derive, stop.

## Execute

Create one dedicated branch/worktree from live `origin/main`. Run the shared preflight.
Execute B0 through B9 exactly in order. Before each batch, declare the role and its file
allowlist. Read every validation result before committing. Do not run `round archive`.

Terminal predecessor evidence is read through GitHub at immutable
`05dd242bf72334bfd683096aed380e8240b6b9aa`; never fetch into `../devai`.

The Inspector must commit reds before the Engineer or Architect fix. PC-0002 is emitted
only after the closure implementation passes inverted/malformed DII tests. Attestation
values are re-read at write time. In B3, establish and test the OM-002 `.devai/`
committed-versus-ignored boundary so runtime state cannot dirty later round worktrees;
R-0005 still owns the full BL-050 lifecycle. Any new defect becomes a gapless Auditor
backlog record before work continues; scope expansion requiring Owner intent stops the
round.

During the pre-review Claude quota pause, OM-004 authorizes bounded,
behavior-focused Inspector tests that improve measured coverage. Do not weaken any
threshold, exclusion, source set, assertion, or production behavior; do not claim
BL-017 closed; and preserve R-0006 ownership of the full 70/60/70/70 gate. Govern any
production defect exposed by the tests before continuing.

OM-005 extends that work until the fresh merged T1+T3 readings reach at least 63.22%
statements, 59.76% branches, 70.58% functions, and 65.48% lines. Re-read raw covered
counts and denominators on each iteration; no displayed-rounding shortcut satisfies
the target. Keep the exact-candidate Claude review suspended until every target is met.

After quota recovery, ask Claude Opus 5 through `claude-opus-5` for a read-only close
review of the exact candidate. Do not fall back to Fable. Resolve its actionable
findings. Push and merge only after all required checks for the SHA are green and the
only remaining remote red is the exact BL-017 coverage job allowed by the shared
contract. Report the overall workflow as red. Execute the shared two-PR closure
ceremony.

## Final report

Return:

`BASE / IMMUTABLE BINDINGS / BATCH COMMITS BY ROLE / ITEMS CLOSED / ITEMS BLOCKING /
PRODUCTION COMMANDS / COVERAGE / PC-0002 / R-0002 CLOSURE / EXACT-MAIN CI / CLAUDE
REVIEW / CLAIMS CEILING / CLEANLINESS`.

The claims ceiling must read: **re-bound and operationally coherent; nothing ratified,
nothing released, no readiness or evidence standing.**
