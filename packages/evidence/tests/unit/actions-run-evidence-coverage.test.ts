// The Actions evidence checks are pure behavior contracts. Project them into
// the T1 lane as well so the governed merged T1+T3 report measures this
// trust boundary without changing its provider, source set, or exclusions.
import '../contract/actions-run-evidence.red.test.js';
