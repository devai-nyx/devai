---
name: devai-docs
description: Create one evidence-grounded developer document at the selected variant's exact output path.
---

# DEVAI docs

Before acting, read the adjacent `devai.recipe.json` and `devai.operations.json`. Select only a declared variant, obey its exact effect and write policy, and invoke only the descriptor's exact operation behavior.

Use this recipe to create exactly one developer document from repository evidence.

1. Select one declared document variant.
2. Inspect the relevant inventory and source evidence.
3. Write only the exact output path declared by the variant; do not accept an alternate or absolute output path.
4. Cite concrete repository artifacts and mark inferences explicitly.
5. Report missing evidence instead of inventing facts.
6. Do not invoke another model or perform remote actions.
