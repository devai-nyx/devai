# @devai-nyx packages publish to / resolve from the org's GitHub Packages
# registry. Auth: NODE_AUTH_TOKEN (a write:packages PAT for the devai-nyx org
# when publishing; read:packages for consumers).
@devai-nyx:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
