---
id: OM-017
title: Pre-R-0007 convergence-control remediation campaign 3 and R-0007 reviewer binding
type: mandate-rider
status: active
date: 2026-07-30
authority: Owner
supersedes: null
superseded_by: null
provenance:
  [
    direct Owner authorization in the pre-R-0007 Codex task on 2026-07-30 to fix every point from remediation campaign 2 Review Run 2,
    allow at most two new substantive review runs,
    use the literal Claude Opus 5 model selector with no fallback,
    avoid repeating prior specimen-only repairs,
    and continue through governed R-0007 closure; OM-002; OM-014; OM-015; OM-016; remediation campaign 2 Review Run 2 failure at 7a39cc31c47a5858e15f2ba3cf7244dd6d744f22,
  ]
related_invariants: []
---

# OM-017 — Pre-R-0007 convergence-control remediation campaign 3 and R-0007 reviewer binding

**Status:** active

**Issued:** 2026-07-30

**Authority:** DEVAI Owner

## Fresh remediation authority

The Owner authorizes one separately named control campaign, **pre-R-0007
convergence-control remediation campaign 3**, after campaign 2 consumed both review
runs and ended in failure at candidate
`7a39cc31c47a5858e15f2ba3cf7244dd6d744f22`.

This campaign is fresh authority, not Review Run 3 of OM-016. It permits one
complete-class repair phase followed by at most two new substantive independent
machinery-review runs. A valid PASS on Review Run 1 ends machinery review. If Review
Run 1 fails, one complete-class repair of every reported class and one complete Review
Run 2 are allowed. Review Run 2 failure returns to Owner escalation. Review Run 3 is
mechanically and procedurally forbidden without another separately named Owner
campaign.

OM-014 through OM-016 remain immutable historical authority for their own campaigns.

## Required complete-class repair population

The campaign must repair all five P1 findings recorded by remediation campaign 2
Review Run 2 together:

1. `RUN2-F001 / AUTHORITATIVE_GATE_CANDIDATE_RED` — every literal authoritative
   command is independently executable from a detached exact-candidate checkout.
   The controller may not repair or inject missing command arguments behind the
   declared roster. Terminal evidence retains all sixteen ordered gate identities and
   exits even after any failure.
2. `RUN2-F002 / AUTHORITATIVE_GATE_FRESHNESS_KEY_INCOMPLETE` — command closure is
   recursively derived through root and all selected workspace package scripts,
   TypeScript project references and outputs, generated outputs, imported or spawned
   executables, environment, toolchain, policy, and history-sensitive inputs. Labels,
   nonempty declarations, and script-name assumptions are not derivation.
3. `RUN2-F003 / AFFECTED_TEST_INPUT_POPULATION_INCOMPLETE` — impact planning reads
   exact candidate and base/preimage Git blobs for added, modified, deleted, copied,
   and renamed inputs. Loader analysis covers direct, aliased, destructured, wrapped,
   applied, bound, optional, computed, eval-based, `createRequire`,
   `require.resolve`, `import.meta.resolve`, and dynamic-import families. Anything not
   proved safe selects full suite and whole coverage.
4. `RUN2-F004 / STATE_AND_TRANSPORT_CHAIN_INCOMPLETE` — each transition authenticates
   the complete exact persisted predecessor artifact, not a selected-field
   reconstruction. Transport authenticates exact payload, validation, state-before,
   prior-attempt, result, count, terminal, and repair-population bytes whenever they
   are consumed. All twelve edges, attempts zero through two, and every terminal are
   executable mutation populations.
5. `RUN2-F005 / REVIEWER_BINDING_CENSUS_NOT_CANDIDATE_BOUND` — a minimal immutable
   bootstrap resolves one literal candidate before version dispatch. Policy, profile,
   schemas, and mandates are loaded from exact candidate objects. Dirty tracked
   substitutions cannot change dispatch, authority, schema, scope, or verdict. Every
   authoritative consumer rejects omitted, symbolic, abbreviated, or ambiguous
   candidate identity.

The prior 69-topic Review Run 2 population, all earlier finding classes, all active
controls, all unchanged semantic obligations, and every current changed path remain
mandatory. No prior PASS or `GREEN_PROVED` label overrides a newly demonstrated
counterexample.

## Anti-repetition and freeze controls

Before Engineer implementation, the Architect must publish a machine-readable
five-class closure matrix and the Inspector must add executable red adversaries for
the full deterministic populations above. The Auditor preserves exact red evidence.
Implementation starts only after the red population exists.

The Engineer repairs all five classes as one phase. No test weakening, source-string
assertion substituted for runtime behavior, hardcoded completeness list substituted
for derivation, worktree byte substituted for candidate byte, or specimen-only fix is
permitted.

Before candidate freeze, the Auditor executes all sixteen literal roster commands from
a clean detached candidate checkout. Every literal argv must PASS without controller
argument injection. Focused contracts, complete tests, tier gates, whole coverage,
sequencing, SHA references, governance, diff, exact candidate/tree, and clean status
must all bind one SHA. Any semantic or current-tree change invalidates that evidence.

Each review starts from a new clean detached candidate-only clone, independently
recomputes every topic and defect-class population, continues after blockers, and
dispositions every changed and unchanged topic exactly once. Every finding includes a
deterministic population query, complete affected population, and machine-checkable
repair condition. Truncated, partial, specimen-only, or unauthenticated output is
invalid rather than PASS.

## Exact reviewer model binding

The sole authorized model selector for both remediation campaign 3 machinery reviews
and the governed R-0007 reviewer slot is:

```text
claude-opus-5
```

Fallback is forbidden. Aliases such as `opus`, selectors resolving to another model,
and silent provider substitution are forbidden. Before each review, the operator must
prove that the invocation reports `claude-opus-5` in model usage. Review evidence must
retain that reported identity. An unavailable model blocks review without consuming a
substantive review run.

This mandate resolves the previously model-neutral R-0007 reviewer slot to exactly one
Owner-authorized selector. No other active mandate may bind a competing selector.

## Publication and R-0007 boundary

After a valid machinery PASS within the two-run ceiling, this campaign may push one
control branch, open one pull request, merge only after exact-head full uncached CI is
green, and verify clean exact-main CI. Remote CI never trusts local freshness state.

The Owner directs continuation into governed R-0007 entry and execution only after the
merged exact-main entry check resolves OM-017 and `claude-opus-5` without ambiguity and
every other existing entry gate passes. R-0007 remains governed by its authorization,
plan, execution contract, role boundaries, batches, two-cycle review ceiling, and stop
conditions. This mandate does not waive or bypass any of them.

Nothing authorizes deployment, package publication, tags, GitHub Releases, evidence
promotion, real-stynx mutation, predecessor mutation, lowered thresholds, weakened
gates, manufactured PASS, or silent reviewer fallback.
