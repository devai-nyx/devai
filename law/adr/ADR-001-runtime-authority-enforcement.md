---
id: ADR-001
title: Runtime authority enforcement
type: adr
status: draft
date: 2026-07-23
authority: Architect (minting = BR-1/W04)
supersedes: [ADR-003-runtime-authority-enforcement.md]
superseded_by: null
provenance: [REV-0003 disposition map; predecessor seeds in law/adr/predecessor/]
affected_rules: [] # TODO at minting
---

# ADR-001. Runtime authority enforcement

## Status

DRAFT (wireframe stub). Binds nothing until minted `accepted` under a declared Architect
session in BR-1/W04. Source texts: ADR-003-runtime-authority-enforcement.md.

## Context

⟦TODO at minting — absorb from predecessor source(s); cite frozen-predecessor evidence by
reference, never restate SHAs/run-IDs.⟧

**Scope (from the disposition map):** The fail-closed authority contract: typed policy, single-use non-replayable decisions, final-adapter boundary (never router-only), no implicit roles or caller-selected machine principals, host-attestation honesty (`cli-only` claims nothing about external tools without a verified adapter).

## Decision

⟦TODO at minting — the binding contract, transposed from source(s) with the deltas below
integrated.⟧

**Verified amendment deltas (dossier Part II — must not be lost):**

- DELTA A1.1 — absorb the zero-exemption mutator source gate (ex-D-136, sole-carried): mechanically derived call-site denominator; zero unauthorized sites, zero exemptions; must demonstrably fail on stale fixtures.
- DELTA A1.2 — absorb the change-control clause (ex-D-136, sole-carried): weakening any fail-closed property requires a new explicit decision; Articles 6-10 changes require constitutional amendment. Delete/rework the predecessor's 'No constitutional amendment is required' line.

## Consequences

⟦TODO at minting.⟧

## Alternatives Considered

⟦TODO at minting — carry forward the predecessor's rejected alternatives where still relevant.⟧

## Affected Rules

⟦TODO at minting — enumerate; feeds the front-matter field and invariant anchor re-pointing (W05).⟧
