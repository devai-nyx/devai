# Architecture decisions

DEVAI v1.0rc does not ship the repository's development-history ADRs. Adopters may add
their own numbered ADRs here; `check --only adrs` validates the current directory without
requiring DEVAI's former sequence.
