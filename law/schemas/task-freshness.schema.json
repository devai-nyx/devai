{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://devai.nyxk.com.br/schemas/task-freshness.schema.json",
  "title": "Content-addressed convergence task result",
  "description": "Runtime-only proof for one local convergence task. Timestamps have no freshness standing.",
  "schema_version": "1.0.0",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "policy_version",
    "task_id",
    "argv",
    "cwd",
    "task_key",
    "input_digest",
    "dependency_keys",
    "toolchain_digest",
    "environment_digest",
    "producing_candidate",
    "outcome",
    "result_digest",
    "outputs"
  ],
  "$defs": {
    "sha1": { "type": "string", "pattern": "^[a-f0-9]{40}$" },
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?:\\.|(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+)$"
    }
  },
  "properties": {
    "schemaVersion": { "const": "1.0.0" },
    "policy_version": { "type": "string", "minLength": 1 },
    "task_id": { "type": "string", "minLength": 1 },
    "argv": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "cwd": { "$ref": "#/$defs/relativePath" },
    "task_key": { "$ref": "#/$defs/sha256" },
    "input_digest": { "$ref": "#/$defs/sha256" },
    "dependency_keys": {
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/sha256" }
    },
    "toolchain_digest": { "$ref": "#/$defs/sha256" },
    "environment_digest": { "$ref": "#/$defs/sha256" },
    "producing_candidate": { "$ref": "#/$defs/sha1" },
    "outcome": {
      "enum": ["EXECUTED_PASS", "SKIPPED_FRESH", "EXECUTED_FAIL", "BLOCKED"]
    },
    "exit_code": { "type": ["integer", "null"] },
    "stdout_sha256": { "$ref": "#/$defs/sha256" },
    "stderr_sha256": { "$ref": "#/$defs/sha256" },
    "result_digest": { "$ref": "#/$defs/sha256" },
    "reused_result_digest": {
      "oneOf": [{ "$ref": "#/$defs/sha256" }, { "type": "null" }]
    },
    "outputs": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "digest"],
        "properties": {
          "path": { "$ref": "#/$defs/relativePath" },
          "digest": { "$ref": "#/$defs/sha256" }
        }
      }
    },
    "freshness_reason": { "type": "string", "minLength": 1 }
  },
  "allOf": [
    {
      "if": { "properties": { "outcome": { "const": "SKIPPED_FRESH" } } },
      "then": {
        "required": ["reused_result_digest", "freshness_reason"],
        "properties": { "reused_result_digest": { "$ref": "#/$defs/sha256" } }
      }
    }
  ]
}
