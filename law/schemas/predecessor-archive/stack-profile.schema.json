{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://devai.dev/schemas/stack-profile.schema.json",
  "$comment": "REDOX-StackProfile: absorbed from /Users/aarusso/Development/tools/redox/src/schemas/StackProfile.schema.json in Phase 17.B (D-57). Complements (does not supersede) repo-introspection.schema.json (Phase 11.D); StackProfile is the broader directory-map output of redox-style detectors, repo-introspection is canonical's lightweight bootstrap detection.",
  "title": "Stack Profile and Directory Map",
  "description": "Coarse-grained stack profile (languages / frameworks / package managers / CI providers / containers / IaC) plus directory map (app / routes / migrations / resources / tests / docs paths) discovered by deterministic detector heuristics. Emitted as a SensorReading body at tier L0 during brownfield introspection (Phase 17.C). Complements `repo-introspection.schema.json` (Phase 11.D) — that schema captures canonical's lightweight `devai init --introspect` bootstrap output; this schema captures the broader directory-map output produced by stack-adapter packs (Phase 17.G). Per Constitution Article 17 (sensors); per D-57 (brownfield-adoption substrate).",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "type": "string", "const": "1.0.0" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "languages": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "frameworks": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "packageManagers": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "ciProviders": {
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "containers": {
      "description": "Containerization tools in use (e.g., docker, docker-compose, k8s).",
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "iac": {
      "description": "Infrastructure-as-code technologies (e.g., Terraform, Kustomize).",
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true,
      "default": []
    },
    "directories": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "app": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": []
        },
        "routes": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": []
        },
        "migrations": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": []
        },
        "resources": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": []
        },
        "tests": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": []
        },
        "docs": {
          "type": "array",
          "items": { "type": "string" },
          "uniqueItems": true,
          "default": []
        }
      },
      "required": ["app", "routes", "migrations", "resources", "tests"]
    }
  },
  "required": ["schemaVersion", "generatedAt", "languages", "frameworks", "packageManagers"]
}
