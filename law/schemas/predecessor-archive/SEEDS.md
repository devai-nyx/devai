These 15 schemas are predecessor copies NOT imported into the DEVAI-II roster
(zero production consumers per the 2026-07-23 audit). Revivable only by a register
entry citing a concrete consumer. Excluded from the roster count, the meta-schema
gate, exports, and compilation. `tombstones` is functionally superseded by the
section-5.1 status lifecycle + `check records`.
