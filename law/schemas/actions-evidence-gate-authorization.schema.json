{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://devai.nyxk.com.br/schemas/actions-evidence-gate-authorization.schema.json",
  "title": "Actions Evidence Gate Authorization",
  "description": "Canonical Architect authorization for ADR-005 Actions evidence promotion. The gate resolver reads this closed record only from the candidate merge's actual base parent; missing, malformed, revoked, or unresolved records select full execution.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "kind",
    "status",
    "decisionId",
    "authorizedAt"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1,
      "description": "Authorization record schema version."
    },
    "kind": {
      "const": "actions-evidence-gate-authorization",
      "description": "Closed discriminator for the ADR-005 gate authorization record."
    },
    "status": {
      "enum": [
        "active",
        "revoked"
      ],
      "description": "Active permits evaluation after every other gate condition passes; revoked selects full execution."
    },
    "decisionId": {
      "type": "string",
      "pattern": "^D-[1-9][0-9]*$",
      "description": "Locked graduation decision that must exist in DESIGN-DECISIONS.md on the same base parent."
    },
    "authorizedAt": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]{3})?Z$",
      "description": "UTC instant when the Architect materialized the authorization."
    }
  },
  "schema_version": "1.0.0",
  "examples": [
    {
      "schemaVersion": 1,
      "kind": "actions-evidence-gate-authorization",
      "status": "active",
      "decisionId": "D-165",
      "authorizedAt": "2026-07-20T15:49:16Z"
    }
  ]
}
